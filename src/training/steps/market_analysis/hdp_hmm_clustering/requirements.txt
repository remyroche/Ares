# Requirements for HDP-HMM Clustering Module
# This module implements Hierarchical Dirichlet Process Hidden Markov Models
# for nonparametric Bayesian regime discovery in financial time series

# Core scientific computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
scikit-learn>=1.0.0

# HMM Libraries (choose one)
# Option 1: ssm-jax (Recommended - Modern, JAX-based, easier to install)
ssm-jax>=0.0.1
jax>=0.4.0
jaxlib>=0.4.0

# Option 2: pyhsmm (Advanced - More features but complex installation)
# Uncomment if you prefer pyhsmm over ssm
# Note: pyhsmm requires Cython and C++ compilers
# Cython>=0.29.0
# git+https://github.com/mattjj/pyhsmm.git

# Visualization and progress tracking
matplotlib>=3.4.0
tqdm>=4.62.0

# Optional: For GPU acceleration with JAX
# jaxlib[cuda11_cudnn82]  # For CUDA 11.x
# jaxlib[cuda12_cudnn88]  # For CUDA 12.x

# Installation Instructions:
# ========================
# 
# Standard installation (CPU only):
# pip install -r requirements.txt
#
# For GPU support with JAX:
# pip install -r requirements.txt
# pip install --upgrade "jax[cuda11_pip]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
#
# Alternative with pyhsmm:
# 1. Install build dependencies:
#    pip install Cython numpy scipy matplotlib
# 2. Install pyhsmm:
#    pip install git+https://github.com/mattjj/pyhsmm.git
#    OR with conda:
#    conda install -c conda-forge pyhsmm
