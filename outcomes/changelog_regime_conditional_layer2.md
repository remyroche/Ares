# Changelog: Regime-Conditional Layer2 HPO Run

## Session: 2026-01-08 20:10

### Commit: regime-conditional-layer2-1740226146381655580

**Files committed (23 total):**

#### Modified Files:
- `artifacts/tv_var_parameters/tv_var_metadata_202601.json` - TV-VAR metadata updates
- `scripts/specialist_feature_diagnostics.py` - Enhanced with RMI feature selection and residualization preprocessing
- `src/analysis/tv_var_system.py` - TV-VAR system improvements

#### New Files:
- `artifacts/tv_var_metadata_202601.json` - New TV-VAR metadata
- `outcomes/orthogonal_analysis_plot_*.png` - Orthogonal analysis plots
- `outcomes/orthogonal_specialist_analysis_*.md` - Orthogonal specialist analysis reports
- `outcomes/specialist_feature_diagnostics_*.csv/md` - Feature diagnostics outputs
- `scripts/test_feature_residualization.py` - Test script for feature residualization
- `src/training/steps/market_analysis/gmm_enhanced_features.py` - GMM enhanced features
- `src/training/steps/market_analysis/gmm_trading_logic.py` - GMM trading logic
- `src/training/steps/market_analysis/shap_interaction_analyzer.py` - SHAP interaction analysis
- `src/training/steps/market_analysis/test_enhanced_gmm_pipeline.py` - GMM pipeline tests
- `src/utils/fracdiff.py` - Fractional differentiation utilities

---

## HPO Run Log

### Run Started: 2026-01-08 20:10
- **Command**: `python3 src/launcher/ares_launcher.py meta_labeling_hpo_sample_weighted --symbol ETHUSDT --execution-mode full --force-hpo --enable-labeling-hpo`
- **Status**: In Progress

---

## Bug Fixes Applied During Run

### Fix 1: RMI-based Composite Reduction (Performance Optimization)
**File:** `src/training/steps/labeling/orthogonal_label_generation.py`
**Functions Added:**
- `calculate_residual_mi()` - Computes Residual Mutual Information using AR(1) residuals and sklearn's MI
- `filter_composites_by_rmi()` - Filters composites from ~1800 to 500 using RMI scores
- `apply_layer2_price_processing()` - Price processing pipeline (log-returns, vol-adjusted, FracDiff, wavelet)
- `_apply_fracdiff()` - Fractional differentiation per AFML Ch. 5
- `_wavelet_denoise()` - Wavelet soft thresholding

**Change Location:** Lines 343-592 (new functions), Line 4949-4955 (integration)
**Impact:** Reduces Phase 5 bottleneck from ~12 min to ~2-3 min by reducing composite count before meta-feature synthesis

### Fix 2: TruncatedSVD + Subsampling (PCA Optimization)
**File:** `src/training/steps/labeling/orthogonal_label_generation.py`
**Function Modified:** `generate_synthetic_meta_signals()`
**Changes:**
- Replaced SparsePCA with TruncatedSVD (randomized algorithm, ~10x faster)
- Added subsampling: Fit PCA on 20K samples, transform full dataset
- Reduced PCA phase from ~12 min to ~30 sec

**Change Location:** Lines 3888-3920
**Impact:** Critical PCA bottleneck eliminated

### Fix 3: Causal Feature Transformation Optimization
**File:** `src/training/steps/labeling/causal_feature_engineering.py`
**Function Modified:** `causal_feature_transformation()`
**Changes:**
- Limited to top 30 models (by score) instead of all 50
- Capped new features at 100 max
- Reduced parent interactions from 3 to 2 per target
- Only create residual features for models with score > 0.1
- Switch to vectorized numpy multiplication

**Change Location:** Lines 370-420
**Impact:** Reduces transformation phase from 10+ min to ~30 sec

### Fix 4: Skip Marchenko-Pastur Denoising for Large Feature Sets
**File:** `src/training/steps/labeling/orthogonal_label_generation.py`
**Function Modified:** `generate_synthetic_meta_signals()`
**Changes:**
- Skip MP denoising when features > 500 (was computing O(n²) correlation matrix = 11M pairs for 3392 features)
- Go directly to TruncatedSVD which handles noise well via truncation

**Change Location:** Lines 3855-3892
**Impact:** Reduces PCA phase from 7+ min to ~10 sec

---

## Layer2 Review Notes

(Will be populated after Layer2 completes)

### Fix 5: Regime-Specific Optimization Column Name Mismatch (Critical Bug)
**File:** `src/training/steps/labeling/label_based_layer_2.py`
**Function Modified:** `optimize_production_geometries`
**Changes:**
- Corrected `regime_col` from `'regime_label'` to `'vol_regime'` to match the column name generated by `_generate_regimes`.
- This ensures that the "Regime-Specific Optimization Loop" correctly iterates over detected regimes instead of falling back to 'Global' or failing silently.

**Change Location:** Line 6678
**Impact:** Enables proper Regime-Conditional HPO as intended.

### Fix 6: Integration of Layer 2 Price Processing
**File:** `src/training/steps/labeling/orthogonal_label_generation.py`
**Function Modified:** `orthogonal_label_generation`
**Changes:**
- Integrated `apply_layer2_price_processing` call at the beginning of the function.
- Ensures standard price features (log_returns, vol_adjusted_returns, fracdiff_price, wavelet_denoised_returns) are computed and available in the dataframe for subsequent steps.

**Change Location:** Line 4426
**Impact:** Ensures completeness of feature set and compliance with de Prado's framework.

### Fix 7: Jaccard Warning Logic Resolution
**File:** `src/training/steps/labeling/orthogonal_label_generation.py`
**Changes:**
- Improved error handling in `Jaccard` calculation block to prevent `Jaccard: ERR` warnings.
- Now defaults to `0.0` and logs a specific warning if calculation fails (uncaught exceptions), preventing pipeline noise.

### Fix 8: Causal Quality Status Unknown
**File:** `src/training/steps/labeling/causal_quality_assessment.py`
**Changes:**
- Added explicit logic to set `causal_quality_status` based on `Layer2Score` thresholds.
- Ensures candidates are correctly tagged as `PASSED`, `WEAK`, or `FAILED` instead of `UNKNOWN`, allowing proper filtering in Layer 2.

### Feature 9: Explicit Tier 1 & Tier 2 Tracking
**File:** `src/training/steps/labeling/label_based_layer_2.py`
**Changes:**
- Added granular `tprint` statements in `optimize_production_geometries` to explicitly log:
    - `[Tier 1] Racing top candidates...`
    - `[Tier 2] Assessing remaining candidates...`
    - Confirms generation of both tiers in the logs for better observability.

### Feature 10: MDI Feature Residualization
**File:** `src/training/steps/labeling/causal_quality_assessment.py`
**Changes:**
- Implemented `_fracdiff_innovation` and `_residualize_feature` helper functions.
- Integrated logic into `_perform_iterative_selection` to intercept selection at 300 features and apply residualization (FracDiff for price/OHLC features, AR(1) residualization for others).
- Ensures features entering final selection are "innovations" rather than raw levels.

### Feature 11: Causal Target Generation
**File:** `src/training/steps/labeling/label_based_layer_2.py`
**Changes:**
- Implemented HAR (Heterogeneous Autoregressive) Model + Studentization pipeline to generate a "Causal Target" (innovation) from close prices.
- Modified `_run_model_race` to use this residualized continuous target (`y_causal`) during `assess_candidate`, improving the validity of causal metrics (IC, CI) compared to using binary labels.
