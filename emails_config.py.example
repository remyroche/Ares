# This is an example configuration file for email settings.
# 1. RENAME this file to `emails_config.py`.
# 2. FILL IN the values below with your actual credentials.
# 3. IMPORTANT: Make sure `emails_config.py` is listed in your .gitignore file
#    to prevent committing sensitive information.

# --- Configuration for Sending Email Notifications (used by ares_mailer.py) ---
EMAIL_CONFIG = {
    'enabled': True,  # Set to False to disable sending emails
    'smtp_server': 'smtp.gmail.com',  # e.g., 'smtp.gmail.com' for Gmail
    'smtp_port': 587,  # e.g., 587 for TLS
    'sender_email': 'your_email@example.com',  # The email address to send from
    'app_password': 'your_google_app_password',  # For Gmail, use an App Password
    'recipient_email': 'recipient_email@example.com',  # The email address to send notifications to
}

# --- Configuration for the Email Command Listener (used by email_command_listener.py) ---
COMMAND_EMAIL_CONFIG = {
    'enabled': True,  # Set to False to disable the command listener
    'imap_server': 'imap.gmail.com',  # e.g., 'imap.gmail.com' for Gmail
    'imap_port': 993,  # e.g., 993 for IMAP SSL
    'email_address': 'your_email@example.com',  # The email account to check for commands
    'app_password': 'your_google_app_password',  # Should be the same as above if using the same account
    'allowed_sender': 'authorized_sender@example.com',  # ONLY emails from this address will be processed
    'polling_interval_seconds': 60,  # How often to check for new emails
}