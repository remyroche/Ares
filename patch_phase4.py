import re

with open("extreme_price_movements/mask_optimiser.py", "r") as f:
    code = f.read()

# I already implemented the Tier-1 generation in the previous step, inside the refactored Phase 3 loop!
# Wait, let's verify if `phase4_single_regime_penalty` is well-implemented in Phase 4.
