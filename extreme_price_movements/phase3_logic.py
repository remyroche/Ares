import numpy as np
import pandas as pd
from typing import Dict, List, Any, Tuple

# ... wait, instead of writing an external module, I will patch `mask_optimiser.py` directly inside `_run_mode_search`.
# This is a large chunk of logic. Let's write the chunk carefully to a file and replace it.
